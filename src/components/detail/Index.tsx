import styled from "styled-components";
import { darkGray, butter, layout } from "../../theme";

const Index = () => {
  return <Section>목차</Section>;
};

const Section = styled.section`
  border: ${layout};
  width: 300px;
`;

export default Index;
